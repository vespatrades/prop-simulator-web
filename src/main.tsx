import React from 'react'
import ReactDOM from 'react-dom/client'
import PropSimulator from './components/PropSimulator'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PropSimulator />
  </React.StrictMode>,
)
